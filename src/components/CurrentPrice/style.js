import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  headerCurrentPrice: {
    width: '100%',
    alignItems: 'center',
    gap: 5,
    paddingBottom: 30,
  },
  currentPrice: {
    color: "#a1810c",
    fontSize: 32,
    fontWeight: 'bold',
    paddingTop: 20,
  },
  textPrice: {
    color: '#fff',
    fontSize: 16,
    fontWeight: 'bold'
  }
})
