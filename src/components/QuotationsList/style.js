import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    flex: 1
  },
  filters: {
    flexDirection: 'row',
    justifyContent: 'center',
    paddingVertical: 15,
    marginBottom: 20,
    gap: 20
  },
  list: {
  }
})
